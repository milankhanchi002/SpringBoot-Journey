spring.application.name=Hospital-Management

spring.datasource.url=jdbc:postgresql://localhost:5432/hospitalDB
spring.datasource.username=postgres
spring.datasource.password=Milankhanchi@123

spring.jpa.hibernate.ddl-auto=update

# To see SQL queries used by Hibernate in the console
spring.jpa.show-sql=true

# Formats the SQL queries for better readability in the console
spring.jpa.properties.hibernate.format_sql=true

spring.jpa.defer-datasource-initialisation=true
spring.sql.init.mode=always
spring.sql.init.data-location=classpath:data.sql